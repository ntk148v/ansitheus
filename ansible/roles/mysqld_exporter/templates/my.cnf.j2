{{ ansible_managed | comment }}
[client]
user={{ mysqld_exporter_username }}
password={{ mysqld_exporter_password }}
{% if mysqld_exporter_target_host %}
host={{ mysqld_exporter_target_host }}
port={{ mysqld_exporter_target_port }}
{% elif mysqld_exporter_target_socket is defined %}
socket={{ mysqld_exporter_target_socket }}
{% endif %}
